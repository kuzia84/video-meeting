# Postgres (docker-compose.yml)
POSTGRES_USER=postgres
# Required — set your own local password (must match the one in
# apps/api/.env DATABASE_URL). Do NOT commit the real value.
POSTGRES_PASSWORD=change-me
POSTGRES_DB=video_meetings
POSTGRES_PORT=5432
